// Логистика. Logistic
// Взят пример из asteriom
//пример: const "кодовое название предмета" = extend("Класс/type", "в кавычках название блока json", {});

const ducts = extend(Duct, "ducts", {});

const ductsjunction = extend(Junction, "ducts-junction", {});
